apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spawnery-docs

# No egress policy: the pod makes no outbound connection at all.
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml
  - networkpolicy.yaml
